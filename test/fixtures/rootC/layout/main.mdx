---
status: active
renderable: false
comment: Master Layout
---

import 'file:///styles/main.scss';

<html lang="en">
    <head>
        <title>{page.title}</title>
        <meta name="description" content="Open Door Go North" />
        <meta charSet="utf-8" />
        { page.cssLinks?.map(c => <link key={c} rel="stylesheet" href={c} />)}
        
    </head>

    <body>{children}</body>

</html>